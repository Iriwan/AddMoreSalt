import { Match } from '../match';
import { Timeline } from '../timeline';
import { Participant } from '../participant';
import { Participantidentity } from '../participantidentity';
import { Team } from '../team';
    export const MATCHES: Match[] = [
      {matchId: 100,  matchMode: "MODE", matchType: "TYPE", queueType: "QUEUE", timeline: null, participants: null,participantIdentities: null, teams: null },
      {matchId: 101,  matchMode: "MODE", matchType: "TYPE", queueType: "QUEUE", timeline: null, participants: null,participantIdentities: null, teams: null }
      ];